package com.accounts.dao.personRus;

import com.accounts.models.MaleLastName;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MaleLastNameDAO extends JpaRepository<MaleLastName, Long> {
}
