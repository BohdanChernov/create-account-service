package com.accounts.dao.personRus;

import com.accounts.models.MaleFirstName;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MaleFirstNameDAO extends JpaRepository<MaleFirstName, Long> {
}
