package com.accounts.dao.personRus;

import com.accounts.models.FemaleLastName;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FemaleLastNameDAO extends JpaRepository<FemaleLastName, Long> {
}
